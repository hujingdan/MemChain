如果你说的是这个原型项目，后续不要直接“边加功能边修”。它现在最大的问题不是功能少，而是原型代码、过期测试、未接入模块、演示数据和真实业务混在一起。建议按“先工程化，再业务闭环，再 AI 增强”的顺序迭代。

目前我已经在这个分支内创建了几个文件夹
- app:后端文件夹，用来存放后端代码
- docs:文档文件夹，用来存放文档
- web:前端文件夹，用来存放前端代码

后续开发按照这个目录来,版本控制使用git分支控制


**总体路线**

保留**MVP2.1**作为当前原型基线，新建一条重构分支，例如 `20260520-hjd-dev_project`。不要先重写所有页面，先把“上传素材 -> 生成元数据 -> 列表展示 -> 预览详情 -> 情绪/标签展示”这条最小闭环稳定下来。

**阶段 0：冻结现状与清理仓库**

目标：让项目能被可靠拉取、安装、运行、测试。

需要先处理这些：

1. 从仓库移除 `frontend/node_modules`、`backend/__pycache__`、`*.pyc`、演示数据库、上传图片。
2. 增加 `.gitignore`：
   ```text
   node_modules/
   __pycache__/
   *.pyc
   *.db
   uploads/
   .env
   dist/
   .pytest_cache/
   ```
3. 保留一份演示数据脚本，而不是提交 [memchain.db](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/backend/memchain.db>)。
4. 修正前端 [package.json](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/frontend/package.json:1>)，至少补：
   ```json
   {
     "scripts": {
       "dev": "vite --host 0.0.0.0",
       "build": "vite build",
       "preview": "vite preview"
     }
   }
   ```
5. 后端明确只支持一个依赖文件，建议删除或弃用当前三引号文档式的 `requirements/base.txt`，保留真实可安装的 `requirements-fixed.txt`，后续改名为 `requirements.txt`。

**阶段 1：统一后端架构**

当前后端入口集中在 [app.py](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/backend/app.py:1>)，可跑但不利于扩展。建议重构成模块化 FastAPI：

```text
backend/
  app/
    main.py
    core/
      config.py
      responses.py
      exceptions.py
      security.py
    db/
      session.py
      base.py
      migrations/
    modules/
      materials/
        model.py
        schema.py
        repository.py
        service.py
        router.py
      emotions/
        model.py
        schema.py
        repository.py
        service.py
        router.py
      ai/
        schema.py
        service.py
        router.py
      users/
        model.py
        schema.py
        service.py
        router.py
  tests/
```

迁移重点：

- 把 [models.py](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/backend/core/data_bunker/models.py:1>) 拆成 `materials/model.py`、`emotions/model.py`、`knowledge/model.py`、`relationships/model.py`。
- 把 [UploadController.py](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/backend/core/data_bunker/controllers/UploadController.py:1>) 改名为 `materials/router.py`。
- 把 [StorageService.py](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/backend/core/data_bunker/services/StorageService.py:1>) 改成真正的存储服务，屏蔽本地磁盘路径细节。
- 把 [timeline_controller.py](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/backend/core/data_bunker/controllers/timeline_controller.py:1>) 拆到 `emotions/router.py` 和 `materials/router.py`，避免“timeline”承担太多职责。
- 统一响应格式，例如：
  ```json
  {
    "success": true,
    "data": {},
    "message": ""
  }
  ```

**阶段 2：重建真实 API 契约**

当前测试和文档引用了不存在的 `/upload/direct`、`/api/ai/analyze`。必须先定 API，不然前后端会继续漂移。

建议稳定为：

```text
POST   /api/materials
GET    /api/materials
GET    /api/materials/{id}
DELETE /api/materials/{id}
GET    /api/materials/{id}/preview

GET    /api/emotions/timeline
POST   /api/emotions

POST   /api/ai/materials/{id}/analyze
GET    /api/ai/materials/{id}
```

第一轮不要做分块上传，先稳定普通上传。等普通上传、预览、删除、AI 分析都稳定后，再加分块上传。

**阶段 3：数据库模型重整**

现在 `materials`、`emotions`、`knowledge`、`relationships` 已经有雏形，但用户、记忆条目、素材、AI 分析结果没有清晰边界。

建议核心表设计：

```text
users
  id, username, password_hash, created_at

memory_entries
  id, user_id, title, content, mood_score, mood_label, weather, happened_at, created_at, updated_at

materials
  id, user_id, entry_id, name, type, mime_type, size, storage_path, checksum, created_at, deleted_at

material_analysis
  id, material_id, provider, model, status, result_json, created_at, updated_at

tags
  id, user_id, name, color

entry_tags
  entry_id, tag_id

relationships
  id, source_material_id, target_material_id, type, strength, metadata, created_at
```

关键点：  
`materials` 只管文件，`memory_entries` 才是用户写下的一段记忆。现在前端“记忆卡片”直接拿文件当记忆，这是 MVP 可以，但长期会限制扩展。

**阶段 4：前端重构**

当前前端路由在 [src/router/index.js](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/frontend/src/router/index.js:1>)，页面上“想法定格”“疗愈空间”“私人宇宙”多数还是占位。建议改为按业务模块组织：

```text
frontend/src/
  api/
    http.js
    materials.js
    emotions.js
    ai.js
  stores/
    user.js
    materials.js
  views/
    corridor/
      MemoryCorridor.vue
    editor/
      EntryEditor.vue
    materials/
      MaterialDetail.vue
    insights/
      InsightCenter.vue
  components/
    upload/
      UploadPanel.vue
    material/
      MaterialCard.vue
      MaterialPreview.vue
    emotion/
      EmotionChart.vue
```

第一轮前端目标：

1. 把 [UploadSection.vue](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/frontend/src/components/UploadSection.vue:1>) 接入“想法定格”页面。
2. 把所有 `fetch()` 封装进 `src/api/*.js`，不要散落在组件里。
3. [MemoryCorridor.vue](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/frontend/src/views/MemoryCorridor.vue:1>) 只负责 UI 和状态，不直接拼 URL。
4. 去掉 `MOCK_LOGGED_IN`，先做本地 demo 用户也要通过统一 store 管理。

**阶段 5：AI 能力分层**

现在 [InsightService.py](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/backend/core/ai_processor/services/InsightService.py:1>) 是规则分析，火山方舟客户端和 [AIController.py](</C:/Users/aut_rain/Documents/Codex/2026-05-20/hujingdan-memchain-https-github-com-hujingdan/MemChain-main/MVP2.1/backend/core/ai_processor/controllers/AIController.py:1>) 是半接入状态。建议不要直接替换，而是分层：

```text
AIProvider
  LocalImageAnalyzer   # Pillow，本地可用
  VolcEngineAnalyzer   # 云端模型
  MockAnalyzer         # 测试用
```

分析结果统一落库到 `material_analysis.result_json`。接口只关心“分析某素材”，不关心背后是本地规则还是云端模型。

第一阶段 AI 只做三件事：

- 图片：主色、亮度、尺寸、简单标签。
- 文本：关键词、情绪分数、摘要。
- 结果缓存：同一个素材不重复分析，除非 `force=true`。

**阶段 6：测试体系重建**

当前 pytest 已经失败，原因是测试导入 `backend.*`，但实际运行目录不是这个包结构。建议重建：

```text
backend/tests/
  conftest.py
  test_health.py
  test_material_upload.py
  test_material_list.py
  test_emotions.py
  test_ai_local_analysis.py
```

最低测试目标：

- `/health` 返回 200。
- 上传一张小图片成功。
- 上传非法类型失败。
- `/api/materials` 能查到上传记录。
- `/api/materials/{id}/preview` 能返回文件。
- 删除素材后数据库和文件状态一致。
- 本地 AI 分析结果可落库。

前端测试可以晚一点，但至少先用 `npm run build` 作为 CI 门槛。

**阶段 7：认证与隐私**

这个项目处理个人记忆，隐私是核心，不是附属功能。

建议第二个大版本再做登录，但设计时先预留：

- `users` 表。
- 所有业务表都有 `user_id`。
- API 查询必须按当前用户过滤。
- 上传目录按用户分区：`uploads/{user_id}/images/...`。
- 不在响应里暴露服务器绝对路径。
- `.env` 存密钥，不进 Git。
- 云端 AI 分析必须显式开关，默认本地分析。

**阶段 8：部署与运维**

原型期建议先用本地 SQLite，进入多人或正式部署再切 PostgreSQL。

推荐部署形态：

```text
frontend: Vite build -> Nginx 静态服务
backend: FastAPI + Uvicorn/Gunicorn
database: PostgreSQL
storage: 本地卷或 S3/MinIO
```

后端配置统一从 `.env` 读取：

```text
DATABASE_URL=
UPLOAD_ROOT=
CORS_ORIGINS=
AI_PROVIDER=local
VOLCENGINE_API_KEY=
JWT_SECRET=
```

**建议迭代节奏**

第 1 周：仓库清理、脚本补齐、后端模块结构调整、真实 API 清单确定。  
第 2 周：完成素材上传/列表/预览/删除闭环，测试通过。  
第 3 周：接入前端上传入口，重构 API 调用层，记忆长廊稳定展示。  
第 4 周：引入 `memory_entries`，把“文件素材”升级成“记忆条目”。  
第 5 周：本地 AI 分析结果落库，详情页展示。  
第 6 周：用户系统、权限隔离、上传目录隔离。  
第 7 周以后：云端 AI、关系图谱、私人宇宙、策展助理。

**最优先改的 5 件事**

1. 修正 `package.json`，让前端能 `npm run dev` / `npm run build`。
2. 删除过期测试和文档里的假接口，按当前真实路由重写。
3. 把上传组件接入“想法定格”，形成真实用户路径。
4. 拆分后端 `models.py` 和 controller/service/repository。
5. 把 AI 分析结果单独建表，不继续塞进 `materials.properties`。

按这个方案推进，项目会从“能展示的原型”逐步变成“可维护的产品骨架”。